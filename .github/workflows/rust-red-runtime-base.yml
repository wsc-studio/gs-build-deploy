name: Build and Push Rust-Red Runtime Base
permissions:
  contents: read
  packages: write
on:
  workflow_dispatch:
  push:
    paths:
      - "Dockerfile.runtime.base"
      - ".github/workflows/rust-red-runtime-base.yml"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: build image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.runtime.base
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/wsc-studio/rust-red-runtime-base:latest
            ghcr.io/wsc-studio/rust-red-runtime-base:${{ github.sha }}
          labels: rust-red-runtime-base
